[metadata]
name = ie_pandas
version = 1.0

# Magic! Don't touch below this line
[options]
install_requires=
    numpy
package_dir=
    =src
packages=find:

[options.extras_require]
dev =
    black
    pytest

[options.packages.find]
where=src
